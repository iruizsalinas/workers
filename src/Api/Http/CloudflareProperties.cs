namespace Workers;

public sealed record Coordinates(float Latitude, float Longitude);
public sealed class Cf;
public sealed class BotManagement;
public sealed class JsDetection;
public sealed class TlsClientAuth;
